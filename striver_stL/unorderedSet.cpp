#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int> st; // Stores elements in unsorted order and accepts only unique values(no duplicates) rest same as set
    // lower bound and upper bound functions do not work
    return 0;
}